import { CallsSortPipe } from './calls-sort.pipe';

describe('CallsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new CallsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
